<lock>
  <id>[% lock.id %]</id>
  <owner>
    <id>[% lock.owner_id %]</id>
    <name>[% lock.owner.username %]</name></owner>
  <expires>[% lock.expiry_time %]</expires>
  <world>
    <id>[% world.id %]</id>
    [% INCLUDE "world/compiled.tt2" %]
  </world>
</lock>
